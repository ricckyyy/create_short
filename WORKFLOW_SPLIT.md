# ワークフロー分割説明

## 概要

GitHub Actions ワークフローを**アカウントごとに分割**しました。これにより、各アカウントの動画を独立して生成できるようになりました。

## 新しいワークフロー構成

### 個別アカウント用ワークフロー（NEW! 🔥）

各アカウント専用のワークフローを作成しました：

1. **`.github/workflows/generate-psychology.yml`** - 心理テストラボ
   - 実行時刻: 毎日 6:00 AM (JST)
   - cron: `0 21 * * *` (UTC)

2. **`.github/workflows/generate-horror.yml`** - 闇夜の語り部
   - 実行時刻: 毎日 6:10 AM (JST)
   - cron: `10 21 * * *` (UTC)

3. **`.github/workflows/generate-cinema.yml`** - 映画紹介
   - 実行時刻: 毎日 6:20 AM (JST)
   - cron: `20 21 * * *` (UTC)

### 既存ワークフロー（継続利用可能）

既存のワークフローも引き続き使用できます：

1. **`.github/workflows/daily-video-generation.yml`** - 全アカウント一括生成（セルフホスト）
2. **`.github/workflows/daily-video-generation-cloud.yml`** - 全アカウント一括生成（クラウド）
3. **`.github/workflows/pr-video-generation.yml`** - PR連動生成（セルフホスト）
4. **`.github/workflows/pr-video-generation-cloud.yml`** - PR連動生成（クラウド）

## メリット

### 1. 独立した実行
- 各アカウントが独立して実行
- 1つのアカウントで問題が発生しても他のアカウントに影響なし

### 2. 時間をずらした実行
- 心理テストラボ: 6:00
- 闇夜の語り部: 6:10
- 映画紹介: 6:20
- サーバーリソースの負荷分散

### 3. 柔軟な制御
- 特定のアカウントだけを手動実行可能
- 特定のアカウントのみを無効化可能

### 4. 短いタイムアウト
- 1アカウントのみ: 30分
- 全アカウント: 60分
- より早く問題を検出

### 5. 明確なログ
- アカウントごとに独立したArtifacts
- ログが見やすく、トラブルシューティングが容易

## 技術的な実装

### 環境変数 `TARGET_ACCOUNT`

`daily_generation.py` は環境変数 `TARGET_ACCOUNT` をサポートします：

```bash
# 特定のアカウントのみ生成
TARGET_ACCOUNT="心理テストラボ" python daily_generation.py

# 全アカウント生成（デフォルト）
python daily_generation.py
```

### ワークフローの例

```yaml
- name: 動画生成スクリプトの実行（心理テストラボ）
  timeout-minutes: 20
  env:
    TARGET_ACCOUNT: "心理テストラボ"
  run: |
    python daily_generation.py
```

## 使用方法

### 手動実行

1. GitHub リポジトリの **Actions** タブを開く
2. 左サイドバーから実行したいワークフローを選択
   - 「動画生成 - 心理テストラボ」
   - 「動画生成 - 闇夜の語り部」
   - 「動画生成 - 映画紹介」
3. **Run workflow** ボタンをクリック
4. **Run workflow** を再度クリックして実行

### 特定アカウントの無効化

特定のアカウントの自動実行を停止する場合：

1. 該当ワークフローファイルを開く
2. `schedule:` セクションをコメントアウト

```yaml
# 自動実行を無効化する例
# schedule:
#   - cron: '0 21 * * *'
workflow_dispatch: # 手動実行は可能
```

### 実行時刻の変更

各アカウントの実行時刻を変更する場合：

```yaml
schedule:
  - cron: '30 21 * * *'  # 6:30 AM (JST) に変更
```

## Artifacts の命名規則

各ワークフローは独自のArtifacts名を使用：

- 心理テストラボ: `videos-psychology-{実行番号}`
- 闇夜の語り部: `videos-horror-{実行番号}`
- 映画紹介: `videos-cinema-{実行番号}`

例: `videos-psychology-123`

## 移行ガイド

### 既存ワークフローからの移行

#### ステップ1: 新ワークフローの有効化
新しいワークフローはデフォルトで有効です。何もする必要はありません。

#### ステップ2: 既存ワークフローの無効化（オプション）
全アカウント一括生成が不要な場合は無効化できます：

```bash
# daily-video-generation.yml の schedule をコメントアウト
```

#### ステップ3: テスト実行
各ワークフローを手動実行してテストします。

## トラブルシューティング

### Q: 複数のワークフローが同時に実行されてしまう
A: 各ワークフローの実行時刻をずらしてください（既にずれています）。

### Q: 特定のアカウントだけエラーになる
A: そのアカウントのワークフローログを確認してください。他のアカウントには影響しません。

### Q: 全アカウントを一度に生成したい
A: 既存の `daily-video-generation.yml` を使用してください。

### Q: PR連動実行で特定のアカウントだけ生成したい
A: `pr-video-generation.yml` を編集して `TARGET_ACCOUNT` 環境変数を追加してください。

## パフォーマンス比較

| 方式 | 実行時間 | 並列性 | 失敗時の影響 |
|------|---------|-------|-------------|
| **アカウント分割** | 20-30分/個 | ✅ 並列実行可能 | ✅ 他に影響なし |
| 全アカウント一括 | 45-60分 | ❌ 順次実行 | ❌ 全体が停止 |

## 推奨設定

### 本番環境
- ✅ アカウント分割ワークフロー使用
- ✅ 時間をずらして実行（既設定済み）
- ✅ 各アカウント30分タイムアウト

### 開発・テスト環境
- ⚠️ 全アカウント一括ワークフロー使用
- ⚠️ PR連動ワークフローで動作確認

## まとめ

ワークフローをアカウントごとに分割することで：

✅ **信頼性向上**: 1つのアカウントの問題が他に影響しない
✅ **パフォーマンス向上**: 負荷分散により安定した実行
✅ **運用性向上**: 個別制御が可能
✅ **トラブルシューティング向上**: ログが明確

---

**作成日**: 2025-12-28
**対応コミット**: 予定
